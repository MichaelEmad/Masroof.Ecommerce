apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-{{ .Chart.Name }}-configmap
data:
  dynamic-env.json: |-
    {
        "production": "true",
        "application": {
            "baseUrl": "{{ include "ecommerce.hosts.angular" . }}",
            "name": "Ecommerce",
            "logoUrl": ""
        },
        "oAuthConfig": {
            "issuer": "{{ include "ecommerce.hosts.httpapi" . }}",
            "redirectUri": "{{ include "ecommerce.hosts.angular" . }}",
            "requireHttps": "true",
            "clientId": "Ecommerce_App",
            "responseType": "code",
            "scope": "offline_access Ecommerce",
            "strictDiscoveryDocumentValidation": false,
            "skipIssuerCheck": true
        },
        "apis": {
            "default": {
                "url": "{{ include "ecommerce.hosts.httpapi" . }}",
                "rootNamespace": "Masroof.Ecommerce"
            }
        }
    }